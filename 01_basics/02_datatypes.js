"use strict"; // treat all JS code as newer version, no commands to reverse this command later in the whole code, whole code need to be written in newer version only

// alert( 3 + 3) // we are using nodejs, not browser, so this will throw error

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object